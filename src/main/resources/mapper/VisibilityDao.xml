<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chandler.eshop.b2b.dao.VisibilityDao">

    <insert id="createVisibility">
        INSERT INTO Visibility (categoryId, groupId, visible)
        VALUES (#{categoryId},#{groupId},#{visible});
    </insert>

</mapper>